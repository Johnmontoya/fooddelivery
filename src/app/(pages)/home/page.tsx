import { Metadata, NextPage } from "next"

export const metadata: Metadata = {
    title: "Directorio",
    description: "Lista de inteligencia artificial",
}

const Home: NextPage = async() =>  {
    return (
        <>
        </>
    )
}